<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Task</title>
    <link 
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" 
    rel="stylesheet">
    <style>
      body {
        background: linear-gradient(to bottom, orange, black);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
    }

        .card {
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            color: #333;
        }

        .btn {
            background-color: orange;
            border: none;
            color: white;
            font-weight: bold;
        }

        .btn:hover {
            background-color: orange;
        }

    </style>
  
</head>
<body>
    <div class="container d-flex justify-content-center align-items-center vh-100">
        <div class="card shadow-sm p-4 " style="width: 200%; max-width: 500px; height:500px;">
          <h1 class="h4 mb-4 text-center mt-2" >Add New Note </h1>
          <form action="/tasks" method="POST">
            <div class="mb-3">
              <label for="taskTitle" class="form-label mt-5">Note title</label>
              <input type="text" class="form-control"  name="title" id="taskTitle" placeholder="Enter title" required>
            </div>
            <div class="mb-3">
              <label for="taskDescription" class="form-label">Note Description</label>
              <textarea class="form-control" name="description" id="taskDescription" rows="3" placeholder="Enter description" required></textarea>
            </div>
            <button type="submit" class="btn w-100 mt-3 mb-3" style="background-color: black; color:orange;">Add Note</button>
          </form>
          <div class="text-center mt-3">
            <a href="/todolist" class="btn w-100 mt-1 mb-3 " style="color:black;">Back to Note</a>
          </div>
        </div>
      </div>
    
      <!-- Bootstrap JS -->
      <script 
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js">
      </script>

</body>
</html>