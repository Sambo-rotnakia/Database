<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Note</title>
    <link rel="stylesheet" href="public/css/index.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <style>
        body {
            background: linear-gradient(to bottom, orange, black);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        h1 {
            margin-bottom: 20px;
            color: #ffffff;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
        }
        .add-task-btn {
            border-radius: 20px;
        }
        .card {
            margin-bottom: 20px;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .card-body {
            background-color: #ffffff;
            border-radius: 12px;
        }
        .container {
            padding-top: 50px;
            flex-grow: 1;
        }
    </style>
</head>
<body>


    <div class="container mt-5">
        <!-- Header -->

        <h1 class="text-center " style="color: black;">Welcome back! Gymnastic 💪🏻🏋🏻‍♀️</h1>
        <h5 class="text-center " style="color: black;">Hope you enjoy your day❤️</h5>

        <!-- Add New Task Button -->
        <div class="d-flex justify-content-end mb-4">
            <a href="/tasks/new" class="btn add-task-btn px-4" style="background-color: black; color:orange;">+ Add New Note</a>
        </div>

        <!-- Task List -->
        <div class="row">
            <% let i=1 %>
            <% tasks.forEach(task => { %>
                <div class="col-md-4">
                    <div class="card shadow-sm">
                        <div class="card-body" >
                            <h5 class="card-title fw-bold text-dark fs-2 mt-2"> <% i++ %> <%= task.title %></h5>
                            <p class="card-text text-muted"><%= task.description %></p>
                            <form action="/tasks/delete/<%= task.id %>" method="POST" style="display:inline;">
                                <button class="btn btn-danger btn-sm" style="margin-top: 100px;" type="submit">Delete</button>
                            </form>
                            <p class="card-text text-muted fs-6"><%= task.due_date %></p>
                        </div>
                    </div>
                </div>
            <% }) %>
            
        </div>
        <a href="/login" class="btn btn-danger btn-sm" style="margin-top: 70px; max-width:100px; background-color:orange; color:black; ">Log Out</a>
    </div>
 
</body>
</html>